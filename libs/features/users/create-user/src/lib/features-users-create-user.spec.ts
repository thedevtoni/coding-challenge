import { featuresUsersCreateUser } from './features-users-create-user';

describe('featuresUsersCreateUser', () => {
  it('should work', () => {
    expect(featuresUsersCreateUser()).toEqual('features-users-create-user');
  });
});
